{
  "name": "forgeai",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "docker compose -f infra/docker/docker-compose.yml up -d postgres && turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
